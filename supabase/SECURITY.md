# Aviso de segurança
#
# Você compartilhou a service_role key neste chat.
# Recomendação: no painel Supabase → Project Settings → API →
# "Reset service role key" depois de concluir o setup, e atualize
# apenas o que for necessário (Edge Functions já usam a key injetada).
#
# Nunca coloque a service_role no frontend ou em commits.
